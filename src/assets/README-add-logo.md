Place the provided SkillVerse PNG logo in this folder with the exact filename `Skill_bazaar_logo.png`.

How to add:
1. Save the image you attached to the workspace as `src/assets/Skill_bazaar_logo.png`.
2. Commit and build.

Notes:
- The app now references `/src/assets/Skill_bazaar_logo.png` in the header, navigation, and loading screen.
- The logo is used throughout the SkillVerse 2026 website.
- If you want the image bundled by Vite (recommended), import it in components instead of using an absolute path.
